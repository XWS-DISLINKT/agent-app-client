version: "3.8"

services:
  agent-app-client:
    image: agent-app-client
    container_name: agent-app-client
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: on-failure
    ports:
      - "5000:4200"
    networks:
      - network

networks:
  network:
    driver: bridge
